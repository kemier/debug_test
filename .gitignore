# Build artifacts
app
app.remote
*.o
*.so
*.a

# Docker build cache
.dockerignore

# IDE files (keep .vscode for debugging config, but ignore backups)
*.bak
.vscode/*.bak

# Temporary files
*.log
*.tmp
/tmp/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Certificates (generated in container)
*.crt
*.key
*.pem

# OpenSSL build artifacts (in server/client/openssl)
server/openssl/
client/openssl/

