# Kind cluster config — 1 control-plane (unschedulable) + 3 workers
# Create with: kind create cluster --config kind-config.yaml --name session-reuse
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker